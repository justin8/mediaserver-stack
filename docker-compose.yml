version: "3"

services:
    vpn:
        image: colinhebert/pia-openvpn
        network_mode: bridge
        privileged: true
        entrypoint:
          - sh
          - -c
          - echo "nameserver 8.8.8.8" > /etc/resolv.conf; openvpn.sh
        environment:
          - "REGION=AU Sydney"
          - "USERNAME=$VPN_USER"
          - "PASSWORD=$VPN_PASS"
        healthcheck:
          test: "wget http://www.google.com -O /dev/null"
    transmission:
        image: linuxserver/transmission
        network_mode: "service:vpn"
        environment:
          - "PGID=${PGID}"
          - "PUID=${PUID}"
        depends_on:
            - vpn
        volumes:
          - $CONFIG_DIR/transmission:/config:rw
          - ${STORAGE_DIR}:${STORAGE_DIR}:rw
        healthcheck:
          test: "curl -ILXGET http://localhost:9091"
    sonarr:
        image: linuxserver/sonarr
        network_mode: "service:vpn"
        environment:
          - "PGID=${PGID}"
          - "PUID=${PUID}"
        depends_on:
          - vpn
          - jackett
        volumes:
          - $CONFIG_DIR/sonarr:/config:rw
          - ${STORAGE_DIR}:${STORAGE_DIR}:rw
        healthcheck:
          test: "curl -ILXGET http://localhost:8989"
    couchpotato:
        image: linuxserver/couchpotato
        network_mode: "service:vpn"
        environment:
          - "PGID=${PGID}"
          - "PUID=${PUID}"
        depends_on:
          - vpn
          - jackett
        volumes:
          - $CONFIG_DIR/couchpotato:/config
          - ${STORAGE_DIR}:${STORAGE_DIR}:rw
        healthcheck:
          test: "curl -ILXGET http://localhost:5050/login"
    jackett:
        image: linuxserver/jackett
        network_mode: "service:vpn"
        environment:
          - "PGID=${PGID}"
          - "PUID=${PUID}"
        depends_on:
          - vpn
        volumes:
          - $CONFIG_DIR/jackett:/config
        healthcheck:
          test: "curl -ILXGET http://localhost:9117"
    mediaserver:
        image: linuxserver/muximux
        network_mode: bridge
        environment:
          - "VIRTUAL_HOST=mediaserver.${DNS_TLD}"
          - "LETSENCRYPT_HOST=mediaserver.${DNS_TLD}"
          - "LETSENCRYPT_EMAIL=${LETSENCRYPT_EMAIL}"
        volumes:
          - $CONFIG_DIR/muximux:/config
          - ./muximux-fix:/etc/cont-init.d/30-muximux-fix
        healthcheck:
          test: "curl localhost -o /dev/null"
    nginx:
        image: justin8/simple-proxy
        network_mode: bridge
        environment:
          - "DNS_TLD=$DNS_TLD"
          - "PROXY_PORT=8080"
          - "PROXY_ENDPOINTS=transmission,vpn:9091;couchpotato,vpn:5050;jackett,vpn:9117;sonarr,vpn:8989"
          - "VIRTUAL_PORT=8080"
          - "VIRTUAL_HOST=transmission.${DNS_TLD},sonarr.${DNS_TLD},couchpotato.${DNS_TLD},jackett.${DNS_TLD}"
          - "LETSENCRYPT_HOST=transmission.${DNS_TLD},sonarr.${DNS_TLD},couchpotato.${DNS_TLD},jackett.${DNS_TLD}"
          - "LETSENCRYPT_EMAIL=${LETSENCRYPT_EMAIL}"
        ports:
          - "${PORT}:${PORT}"
        depends_on:
          - vpn
        links:
          - vpn
